# Network boundary

The Clyvora website loads its interface and cookie-free Vercel page analytics from its own origin. It does not load third-party fonts or advertising and it never receives selected file contents from Lens or Convert.

Outbound links are followed only when a visitor selects them. Product-specific exceptions, such as Convert's user-requested link import, are documented in each product repository.
